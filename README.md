# Cure — Landing de reserva de restaurante (React)

Landing page para reservar mesa en un restaurante, hecha con **React + Vite**.

## Desarrollo local

```bash
npm install
npm run dev
```

Abre `http://localhost:5173`.
